-- Crew-named config alias. Spawn tuning remains in BrainrotSpawnSettings until
-- the model rollout and save-data migration are validated.
return require(script.Parent:WaitForChild("BrainrotSpawnSettings"))
